# T1053.005 — Scheduled Task

## Atomic
#2 Scheduled task Local — schtasks.exe /Create /TN spawn /TR cmd.exe

## Telemetry
Sysmon EID 1 (process creation), groups: sysmon_eid1_detections
Chain: cmd.exe -> schtasks.exe
Key fields: image, commandLine, parentImage

## My rule
100101, level 10
Matched on image=schtasks.exe AND commandLine contains /Create
Chose /Create over image alone — querying tasks is routine,
creating them is the persistence action.

## Notes
No backslash problem here — /Create has no path separators.
Ran clean first try, ~20 min vs several hours for T1547.001.
